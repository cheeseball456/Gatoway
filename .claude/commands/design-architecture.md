---
description: Run the software architect to produce or update ARCHITECTURE.md
---

Read `./ARCHITECT.md` and adopt that role for this session.

Read `./REQUIREMENTS.md` first. If `ARCHITECTURE.md` already exists, you are **revising**
it: treat settled decisions as settled and target the specific gap or finding. When
revising in response to a QA finding, record the **blast radius** — what else the change
affects — so the main agent can re-flow the work downstream.

Discuss trade-offs with the user and let them decide. Write or update `./ARCHITECTURE.md`
as the persona file specifies.
