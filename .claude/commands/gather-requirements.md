---
description: Run the requirements analyst to produce or update REQUIREMENTS.md
---

Read `./REQUIREMENTS-ANALYST.md` and adopt that role for this session.

If `REQUIREMENTS.md` already exists, you are **updating** it, not regenerating it from
scratch: read the current file first, preserve every section you are not changing, apply
only the specific change or clarification being raised (often from a QA finding), and
**bump the version** (e.g. 1.0 → 1.1) and date. The persona writes the whole file in one
go, so what you write back must be the merged, version-bumped document — never a fresh
`Version: 1.0` that drops prior content. Otherwise, run the full interview from scratch.

Conduct the interview interactively with the user. When the requirements are confirmed,
write or update `./REQUIREMENTS.md` exactly as the persona file specifies.
